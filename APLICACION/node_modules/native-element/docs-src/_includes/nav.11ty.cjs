const relative = require('./relative-path.cjs');

module.exports = function({page}) {
  return `
<nav>
  <a href="${relative(page.url, '/')}">首页</a>
  <a href="${relative(page.url, '/examples/')}">例子</a>
  <a href="${relative(page.url, '/api/')}">API</a>
  <a href="${relative(page.url, '/install/')}">安装</a>
</nav>`;
};
